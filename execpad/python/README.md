# agentpad (Python)

This directory is the **Python** distribution of [agentpad](../README.md). The **canonical README** (install for npm + pip, quick starts, features, and docs index) lives at the **repository root**: [`../README.md`](../README.md).

```bash
pip install -e ./python   # from clone
pytest                    # with pip install -e '.[dev]'
```

Python-specific reference: **[slaps.dev/docs/agentpad/python](https://slaps.dev/docs/agentpad/python)**.
